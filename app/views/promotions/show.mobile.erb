<%= @promotion.mark_as_read %>
<% Notification.where('playerid = ? and notificationid=? and notification=?' , current_player.playerid, @promotion.id, 'promotions').each do |n| %>
	<% n.set_bulb_to_zero %>
<% end %>

<% Notification.mark_as_read(current_player.playerid, @promotion.id, 'promotions') %>